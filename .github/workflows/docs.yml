name: Deploy Docs

on:
  push:
    branches: ["main"]
    paths:
      - "docs/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MDBOOK_VERSION: "0.5.4"
      MDBOOK_KATEX_VERSION: "0.10.0-alpha"
      MDBOOK_KATEX_RELEASE_TAG: "0.10.0-alpha-binaries"
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install mdBook
        run: |
          mkdir -p bin
          curl -sSL "https://github.com/rust-lang/mdBook/releases/download/v${MDBOOK_VERSION}/mdbook-v${MDBOOK_VERSION}-x86_64-unknown-linux-gnu.tar.gz" | tar -xz -C bin
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: Install mdbook-katex
        run: |
          curl -sSL "https://github.com/lzanini/mdbook-katex/releases/download/${MDBOOK_KATEX_RELEASE_TAG}/mdbook-katex-v${MDBOOK_KATEX_VERSION}-x86_64-unknown-linux-gnu.tar.gz" -o /tmp/mdbook-katex.tar.gz
          file /tmp/mdbook-katex.tar.gz | grep -q gzip || { echo "::error::Downloaded file is not a gzip archive - check MDBOOK_KATEX_RELEASE_TAG and MDBOOK_KATEX_VERSION against https://github.com/lzanini/mdbook-katex/releases"; cat /tmp/mdbook-katex.tar.gz; exit 1; }
          tar -xzf /tmp/mdbook-katex.tar.gz -C bin
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: Build Documentation
        run: mdbook build docs/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
